This is a proxy for the stream server capability of Enigma2 receivers.

Transcoding is performed through `ffmpeg`.

This was designed to be built as a Docker image. See Nodejs package in `rootfs/app/`.
